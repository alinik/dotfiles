Setup:

```bash
git clone http://git.baregheh.com/scm/ext/oh-my-zsh.git
alias config='git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'
git clone --bare http://git.baregheh.com/scm/~nikneshan/dotfiles.git .dotfiles
config reset --hard

src
```

